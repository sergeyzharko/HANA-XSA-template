components:
  parameters:
    SearchParam:
      in: query
      name: search
      required: false
      schema:
        type: string
      description: value for searching data by any column